<!DOCTYPE html>
<html>
  <head>
    <title>RateAHero</title>
    <%= csrf_meta_tags %>
    <link rel="stylesheet" type="text/css" href="   https://cdnjs.cloudflare.com/ajax/libs/foundation/6.4.3/css/foundation-float.css"/>
    <script src="   https://cdnjs.cloudflare.com/ajax/libs/foundation/6.4.3/css/foundation-float.css"></script>
    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
  </head>

  <body>
    <div class="top-bar">
        <h1 class="menu-text">Rate-a-Hero</h1>
    </div>
    <nav>
        <% if user_signed_in? %>
          <button id="devise"><%= link_to 'Edit Account', edit_user_registration_path %></button>
          <button id="devise"><%= link_to 'Sign Out', destroy_user_session_path, method: :delete %></button>
        <% else %>
          <button id="devise"><%= link_to 'Sign In', new_user_session_path %></button>
          <button id="devise"><%= link_to 'Sign Up', new_user_registration_path %></button>
        <% end %>
    </nav>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

      <%= yield %>
    </div>
  </body>
</html>
